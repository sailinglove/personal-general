#include <iostream>
using namespace std;

#define MAX_N 16

//global variables
int array[MAX_N][MAX_N] = {0};

int main()
{
    int n = 0;
    cin >> n;
    for (int i = 0; i <= 2 * n; i++)
    {
        
    }
}