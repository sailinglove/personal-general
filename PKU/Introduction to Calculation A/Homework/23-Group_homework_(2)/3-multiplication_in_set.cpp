#include <iostream>
using namespace std;

//global variables

int subset(int number, int mode = 0){
    if (mode = 0){
        
    }
}


int main() {
    int T = 0, N = 0;
    int S[17] = {0};
    int *S_ptr = &S[0];

    cin >> T >> N;

    for (S_ptr = &S[0]; S_ptr < (S + N); S_ptr++){
        cin >> *S_ptr;
    }

    

    return 0;
}